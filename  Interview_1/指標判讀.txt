int *a;
                a是一個 指向int型別的指標 
int **a;
                a是一個 指向int型別的指標的指標
int a[10];
                a是一個 陣列擁有10個int型別的data
int *a[10];
                a是一個 指標指向 陣列擁有10個int型別的data
int (*a)[10];

int (*a)(int);

int (*a[10])(int);

const int *foo; 

int const *foo;

int* const foo; 

int const *const foo; 
